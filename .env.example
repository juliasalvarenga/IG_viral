# -----------------------------------------------------------------------
# Groq API Key (FREE — sign up at https://console.groq.com)
# -----------------------------------------------------------------------
GROQ_API_KEY=your_groq_key_here

# -----------------------------------------------------------------------
# Instagram credentials (for instaloader — NOT a paid API key)
# Recommended: create a separate "burner" IG account for scraping
# Leave blank to use anonymous access (more aggressive rate-limiting)
# -----------------------------------------------------------------------
IG_USERNAME=your_instagram_username
IG_PASSWORD=your_instagram_password

# -----------------------------------------------------------------------
# Optional: Google Sheets export
# -----------------------------------------------------------------------
# Path to your Google service account JSON credentials file
GOOGLE_SERVICE_ACCOUNT_FILE=credentials.json
# The ID of your Google Sheet (from its URL)
GOOGLE_SHEET_ID=your_sheet_id_here

# -----------------------------------------------------------------------
# Scraper defaults (all can be overridden via CLI flags)
# -----------------------------------------------------------------------
# Comma-separated hashtags or @profiles to scrape
SCRAPE_TARGETS=fitness,motivation,entrepreneur
# Minimum view count to include a reel
MIN_VIEWS=1000000
# Maximum number of reels to pull per target
MAX_REELS_PER_TARGET=20
# Number of scripts to generate
SCRIPTS_TO_GENERATE=10

# -----------------------------------------------------------------------
# Model settings
# -----------------------------------------------------------------------
# Groq model — free options: llama-3.3-70b-versatile | mixtral-8x7b-32768 | gemma2-9b-it
GROQ_MODEL=llama-3.3-70b-versatile

# Local Whisper model size: tiny | base | small | medium | large
# base = good balance of speed and accuracy on CPU
WHISPER_MODEL_SIZE=base
