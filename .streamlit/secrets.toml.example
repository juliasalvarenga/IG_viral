# Copy the contents of this file into your Streamlit Cloud app's
# Settings → Secrets panel (not as a file — paste the text directly).
#
# For local development, use a .env file instead (see .env.example).

# Required
GROQ_API_KEY = "gsk_your_groq_key_here"

# Recommended — use a burner Instagram account to avoid rate-limiting
IG_USERNAME = "your_instagram_username"
IG_PASSWORD = "your_instagram_password"
